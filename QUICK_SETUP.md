# 快速配置指南 - 一步一步来

## 📝 第三步：获取 Supabase 密钥

### 在 Supabase 网站操作：

1. **打开你的 Supabase 项目页面**
   - 应该能看到项目 Dashboard

2. **点击左侧边栏的 "Settings"**
   - 图标是齿轮 ⚙️
   - 在左侧菜单的最下面

3. **点击 "API"**
   - 在 Settings 页面中

4. **复制两个值**：
   
   **第一个：Project URL**
   - 在页面顶部，标签是 "Project URL"
   - 值类似：`https://abcdefghijklmnop.supabase.co`
   - 点击旁边的复制按钮 📋

   **第二个：anon public key**
   - 在 "Project API keys" 部分
   - 找到 "anon" 和 "public" 这一行
   - 点击 "Reveal" 显示密钥（如果被隐藏）
   - 点击复制按钮 📋
   - 这是一个很长的字符串，以 `eyJ` 开头

5. **把这两个值保存到记事本**，下一步要用

---

## ⚙️ 第四步：配置项目

### 步骤 1：创建 .env.local 文件

**最简单的方法 - 使用终端**：

1. **打开终端**（Terminal）
   - Mac: 按 `Cmd + 空格`，输入 "Terminal"，回车
   - 或者：应用程序 → 实用工具 → 终端

2. **进入项目目录**：
   ```bash
   cd /Users/k/Desktop/Player
   ```

3. **创建文件**（复制粘贴这一行）：
   ```bash
   cat > .env.local << 'EOF'
   NEXT_PUBLIC_SUPABASE_URL=你的_Project_URL
   NEXT_PUBLIC_SUPABASE_ANON_KEY=你的_anon_public_key
   EOF
   ```

4. **编辑文件，替换值**：
   ```bash
   open -e .env.local
   ```
   这会用文本编辑器打开文件

5. **在编辑器中**：
   - 把 `你的_Project_URL` 替换成你从 Supabase 复制的 Project URL
   - 把 `你的_anon_public_key` 替换成你从 Supabase 复制的 anon key
   - 保存文件（`Cmd + S`）

**或者手动创建**：
1. 在项目文件夹中，创建新文件
2. 文件名：`.env.local`（注意前面有个点）
3. 打开文件，粘贴：
   ```
   NEXT_PUBLIC_SUPABASE_URL=你的_Project_URL
   NEXT_PUBLIC_SUPABASE_ANON_KEY=你的_anon_public_key
   ```
4. 替换成实际值
5. 保存

### 步骤 2：安装依赖

在终端中（确保在项目目录 `/Users/k/Desktop/Player`）：

```bash
npm install @supabase/supabase-js
```

**等待完成**，你会看到类似：
```
added 1 package, and audited XXX packages
```

### 步骤 3：重启服务器

1. **如果服务器正在运行**：
   - 找到运行 `npm run dev` 的终端窗口
   - 按 `Ctrl + C` 停止
   - 然后运行：
     ```bash
     npm run dev
     ```

2. **如果服务器没有运行**：
   - 直接运行：
     ```bash
     npm run dev
     ```

---

## ✅ 验证成功

打开浏览器访问 `http://localhost:3000`，应该能看到：
- 绿色提示框："云端同步已启用 - 文件将在所有设备间同步"

---

## 🆘 需要帮助？

告诉我你卡在哪一步，我会详细解释！





